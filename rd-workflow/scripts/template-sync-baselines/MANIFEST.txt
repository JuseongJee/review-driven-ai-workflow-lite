# template-sync allowlist manifest
# test_template_sync.sh 가 읽는 lite(_ROOT_FILES_LITE) 의도된 차이 목록.
# 형식: [section] 헤더 + 엔트리(상대경로, hooks/ 기준). '#' 주석·빈 줄 무시.
#
# - lite-absent      : 루트에만 존재해야 정상 (lite 미포함 의도). lite에 있으면 fail.
# - lite-content-diff: 양쪽 존재, 의도된 내용 차이. lite/<name>.diff baseline 과 대조.
# - lite-pending     : 타 FR 동기화 대기 중 임시 등록 (fr= 링크 필수). baseline 대조하되,
#                      담당 FR 완료로 diff 가 바뀌면 baseline 불일치 fail → 엔트리 제거 신호.

[lite-absent]
hooks/overlay_branch_sync.sh
hooks/pre_commit_verify.sh
hooks/test_overlay_branch_sync.sh
hooks/test_pre_commit_verify.sh

[lite-content-diff]
hooks/session_start.sh
