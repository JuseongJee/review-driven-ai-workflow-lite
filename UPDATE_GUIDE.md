# 템플릿 업데이트 가이드 (zip 배포용)

새 zip을 받았을 때 Claude Code에서:

```text
이 템플릿으로 업데이트해: ~/Downloads/rd-workflow-lite.zip
```

Claude가 압축 해제 → 파일 분류 → 사용자 확인 → 동기화를 자동으로 진행합니다.

## 버전 확인

현재 프로젝트의 템플릿 버전:

```bash
cat rd-workflow/VERSION
```
